spring.application.name=email-service

#AWS Ses
aws.accesskey=${AWS_ACCESS_KEY}
aws.secretkey=${AWS_SECRET_KEY}
aws.region=${AWS_REGION}

# Input the same AWS IAM user e-mail
email=${EMAIL}

#swagger
springdoc.api-docs.path=/api-docs